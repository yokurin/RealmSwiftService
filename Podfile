# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Example' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Example
  pod 'RealmSwift'
end

target 'RealmSwiftService' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RealmSwiftService
  pod 'RealmSwift'

  target 'RealmSwiftServiceTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
